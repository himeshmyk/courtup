{
  "name": "courtup",
  "version": "0.1.0",
  "private": true,
  "description": "CourtUp — sports venue booking + social games PWA",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "db:setup": "npm run db:setup -w apps/api",
    "start": "npm run start -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
